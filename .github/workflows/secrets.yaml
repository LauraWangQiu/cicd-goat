name: Secret Scanning

on:
  pull_request:
    branches: [ "main" ]

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: LauraWangQiu/cicd-security-scanner@v1
        with:
          base_ref: main
