name: Secret scanning

on:
  pull_request:
    branches: [ "main" ]

jobs:
  secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Build scanner image
        run: docker build -t cicd-security-scanner .

      - name: Run secret scanning
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/scan \
            -e GITHUB_BASE_REF=${{ github.base_ref }} \
            -e GITHUB_HEAD_REF=${{ github.head_ref }} \
            -e GITHUB_SHA=${{ github.sha }} \
            cicd-security-scanner

      - name: Prepare artifact contents
        if: always()
        run: |
          mkdir -p artifact
          cp reports/* artifact/
          {
            echo "repository=${{ github.repository }}"
            echo "commit_sha=${{ github.sha }}"
            echo "commit_url=https://github.com/${{ github.repository }}/commit/${{ github.sha }}"
          } > artifact/metadata.txt

      - name: Upload secret scanning reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: ${{ github.sha }}
          path: artifact

      - name: Comment on PR if secrets found
        if: failure()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `⚠️ **Secret Scanning Alert** ⚠️\n\n🔐 Potential secrets have been detected in this PR!\n\n**Action Required:**\n- ❌ Do NOT merge this PR\n- 🔄 **Rotate the exposed secret immediately** in your secret management system\n- 🧹 Remove the secret from the code\n- 📝 Force push to update the commits\n- ✅ Re-run the scan to verify\n\nFor more details, check the [artifact reports](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).\n\n*This is an automated message from the Security Bot.*`
            })